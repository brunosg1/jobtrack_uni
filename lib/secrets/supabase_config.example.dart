/// Example Supabase configuration file for package import.
///
/// Copy this file to `secrets/supabase_config.dart` (outside `lib`) and fill
/// in your real values. Do NOT commit the `secrets/supabase_config.dart` file.

class SupabaseConfig {
  static const String url = 'https://REPLACE_WITH_YOUR_PROJECT.supabase.co';
  static const String anonKey = 'REPLACE_WITH_YOUR_ANON_KEY';
}
